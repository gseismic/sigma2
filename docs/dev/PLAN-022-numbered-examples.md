# PLAN-022：示例文件按序号命名

更新时间：2026-09-24 12:04 CST

状态：已完成

## 目标

按用户要求，将六个可运行示例改为 `01_xx.py`、`02_xx.py` 等顺序命名，并保持 README、skill 和运行命令一致。

## 实施步骤

1. 按 README 的展示顺序将六个 `examples/*.py` 脚本重命名为 `01_` 到 `06_` 前缀，保留示例内容和顺序。
2. 更新示例顶部命令、README 和 `skills/sigma2-usage/` 的文件链接及命令；保留 PLAN-021 历史记录的原貌。
3. 检查新命令与文档链接，审阅变更，记录结果并追加索引；只提交本次相关文件，然后立即推送。

## 完成标准

- `examples/` 六个脚本名称均按两位序号排序。
- README 和 skill 不再引用旧文件名，文档命令可从仓库根目录运行。
- 不提交现有未跟踪软链接。

## 执行摘要

- 六个示例按 README 顺序重命名为 `01_` 至 `06_`，脚本计算逻辑未改。
- 同步更新 README、skill 和每个脚本顶部的运行命令；PLAN-021 历史记录保持原样。
- 新的 `python -m` 命令可执行，六个编号模块可定位，文档链接和 Ruff 检查通过。
